# Todo list
- FIX NODEATH SEGFAULT
- build system
	- switch to cmake
	+ configure script
	+ install and uninstall target
	- separate everything project specific
- objects
	- free
	= obj:
		= position
		= rotation
			- need quaternions
		= scale
		= momentum
		= anglular momentum
		= target rotation

		+ obj make(void);
	= globj:
		= transformation matrix
		+ globj load(obj);
	- rework objs
- models
	- free
	= seq
		- figure this out;
	- materials and textures
- image loading
	+ choose library
		= stb_image.h
	+ basic implementation
	+ wip_img wrapper
- graphics
	+ MSAA
	+ UV maps
	- normal maps
		- loading
		- shaders
- window
	- dynamic size
- UI
	- font rendering
		? bitmap fonts
		? freetype
	- fps/frametime meter
	? wip_ui
- input
	+ handling
	+ mapping
	+ motions
	+ rebinding
	- SDL2 support
	- mouse inputs
- settings
	+ xdg config
	+ config loading
	+ setting storage
	+ resolution
	+ fullscreen
	+ vsync
	+ antialiasing
	+ input rebinding
		- fix alpha keys
			? check on this
	- fix include directive in config
- logic loop
	- move rendering to logic loop, render loop is dumb
	- move logic loop to main thread
	- move init code from main
	- decouple movement from tickrate (demo, possibly nodeath)
- change some errors to warnings
- commandline arguments
- audio
	? openAL
	? something simpler
